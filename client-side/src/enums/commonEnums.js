export const ConfirmActionType = Object.freeze({
  DELETION: "Deletion",
  APPROVAL: "Approval",
});

export const ConfirmActionWords = Object.freeze({
  Deletion: "delete",
  Approval: "approve",
});
