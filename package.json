{
  "name": "psits-web",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix server-side\" \"npm run dev --prefix client-side-ts\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5",
    "prettier": "^3.7.4",
    "prettier-plugin-tailwindcss": "^0.7.2"
  },
  "dependencies": {
    "express-async-errors": "^3.1.1",
    "react-icons": "^5.7.0"
  }
}
